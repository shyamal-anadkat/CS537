#include "types.h"
#include "user.h"

extern int main (int argc,char* argv[]);

void _start(int argc,char* argv[]) {
	    int retVal;
	    retVal = main(argc,argv);
	    exit(retVal);
	    return;
}
 
